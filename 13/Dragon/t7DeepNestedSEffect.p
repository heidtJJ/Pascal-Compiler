program main( input, output );
  var a: integer;
  var y: integer;
  var p: real;
  var q: integer;
  var fuck: integer;


  function foo( a: integer; x: real; z: integer ): integer;
    procedure boo( a: real; b: integer );
      procedure yes(a: real);
      begin
        p := 1.0;
        a := p;
        q := 4;
        b := q;
        foo = 5;
      end;
    begin
      fuck := y + z;
      yes(1.0)
    end;
  begin
    boo( 1.0, 1 );	
    foo := a + y;
    boo(x, y);
  end;

begin
  for y := 1 to 5 do
    a := 1;
    fuck := a;
end.

